# These symbols are defined in the dryncs script to make them more readable.
# Dialog program is called, but this is pretend for called to xdialog,zenity too, in future version of dryncs

#: "${DIALOG=dialog}"

#: "${DIALOG_OK=0}"
#: "${DIALOG_CANCEL=1}"
#: "${DIALOG_HELP=2}"
#: "${DIALOG_EXTRA=3}"
#: "${DIALOG_ITEM_HELP=4}"
#: "${DIALOG_TIMEOUT=5}"
#: "${DIALOG_ESC=255}"

#: "${SIG_NONE=0}"
#: "${SIG_HUP=1}"
#: "${SIG_INT=2}"
#: "${SIG_QUIT=3}"
#: "${SIG_KILL=9}"
#: "${SIG_TERM=15}"
